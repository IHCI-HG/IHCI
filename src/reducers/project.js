const projectInitialState = {
    name: "",
    publisher: "",
    
}

const project = (state = projectInitialState, action) => {
    switch (action.type) {
        case 'aaa':
            return state
    
        default:
            return state
    }
}

export default project
